import { ShapeSelect } from './shape-select';

describe('ShapeSelect', () => {
  it('should create an instance', () => {
    const directive = new ShapeSelect();
    expect(directive).toBeTruthy();
  });
});
