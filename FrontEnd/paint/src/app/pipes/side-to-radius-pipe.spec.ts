import { SideToRadiusPipe } from './side-to-radius-pipe';

describe('SideToRadiusPipe', () => {
  it('create an instance', () => {
    const pipe = new SideToRadiusPipe();
    expect(pipe).toBeTruthy();
  });
});
